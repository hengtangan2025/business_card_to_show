.desc
  %span
  - if current_user != nil
    %a.to-questions{:href => '/business_cards'} 查看名片 （点击跳转到current_user的名片）
    %a.to-questions{:href => '/business_cards/#{id}/edit'}修改名片 （点击跳转到名片的edit页面）
    = "创建名片：我的想法是注册用户成功后用回掉方法同时为该用户创建一个空名片"

.article-index
  %ul.articles
    - @articles.each do |article|
      %li.article-name
        %a{:href => "/articles/#{article.id}"}= article.title
        (点击标题进入文章详情页)
        = image_tag(article.picture)